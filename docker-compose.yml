version: "3.9"

services:

  churn-api:

    build: .

    image: riyanozair/churn-ml-app

    container_name: churn-ml-system

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    restart: always